services:
  paygate:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - paygate_data:/app/pb_data
    restart: unless-stopped

volumes:
  paygate_data:
